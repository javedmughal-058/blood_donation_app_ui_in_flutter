import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class donor_screen extends StatelessWidget {
  const donor_screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
